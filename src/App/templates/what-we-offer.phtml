<?php
$layout('layout::tailwind', [
    'title' => 'What we offer - responsive.sk',
    'description' => 'Our team can provide a bespoke package to suit your individual requirements.',
    'body_class' => 'what-we-offer-page',
    'cssUrl' => $cssUrl ?? null,
    'jsUrl' => $jsUrl ?? null,
]);
?>

<!-- Hero Section -->
<div class="bg-white py-16 sm:py-24">
    <div class="max-w-7xl mx-auto px-6 lg:px-8">
        <div class="max-w-3xl">
            <h1 class="text-4xl font-bold tracking-tight text-gray-900 sm:text-6xl">
                What we offer
            </h1>
            <p class="mt-6 text-lg leading-8 text-gray-600">
                Our team can provide a bespoke package to suit your individual requirements. We can be the extra people you need on an existing or standalone project. We can mentor your team of developers to share our expertise, or we could help to modernise your software to keep up with this ever-changing industry.
            </p>
        </div>
    </div>
</div>

<!-- Services Section -->
<div class="bg-gray-50 py-16 sm:py-24">
    <div class="max-w-7xl mx-auto px-6 lg:px-8">
        <div class="space-y-16">
            <?php foreach ($services as $index => $service): ?>
                <div class="grid lg:grid-cols-2 gap-12 items-center <?= $index % 2 === 1 ? 'lg:grid-flow-col-dense' : '' ?>">
                    <!-- Content -->
                    <div class="<?= $index % 2 === 1 ? 'lg:col-start-1' : '' ?>">
                        <h2 class="text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">
                            <?= $e($service['title']) ?>
                        </h2>
                        <p class="mt-4 text-lg text-gray-600">
                            <?= $e($service['description']) ?>
                        </p>
                        
                        <!-- Features -->
                        <ul class="mt-8 space-y-3">
                            <?php foreach ($service['features'] as $feature): ?>
                                <li class="flex items-start">
                                    <svg class="h-6 w-6 text-green-500 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                                    </svg>
                                    <span class="ml-3 text-gray-700"><?= $e($feature) ?></span>
                                </li>
                            <?php endforeach; ?>
                        </ul>
                    </div>
                    
                    <!-- Image -->
                    <div class="<?= $index % 2 === 1 ? 'lg:col-start-2' : '' ?>">
                        <div class="aspect-[4/3] overflow-hidden rounded-2xl bg-gray-100">
                            <img src="<?= $e($service['image']) ?>" 
                                 alt="<?= $e($service['title']) ?>" 
                                 class="h-full w-full object-cover">
                        </div>
                    </div>
                </div>
            <?php endforeach; ?>
        </div>
    </div>
</div>

<!-- CTA Section -->
<div class="bg-white py-16 sm:py-24">
    <div class="max-w-7xl mx-auto px-6 lg:px-8">
        <div class="max-w-2xl mx-auto text-center">
            <h2 class="text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl">
                Whatever you Need
            </h2>
            <p class="mt-4 text-lg text-gray-600">
                We can be flexible to your needs, so if there's something that you need help with, or you'd like to discuss your requirements with us, don't hesitate to get in touch.
            </p>
            <div class="mt-8">
                <a href="<?= $url('app::contact') ?>" 
                   class="inline-flex items-center px-8 py-3 bg-black hover:bg-gray-800 text-white font-medium text-lg rounded-full transition-all duration-200 hover:shadow-lg">
                    Let's Talk
                </a>
            </div>
        </div>
    </div>
</div>
