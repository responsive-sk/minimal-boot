<?php
/**
 * @var array $data Template data
 * @var string $title Page title
 * @var string $module Module name
 */
// Use theme-aware layout - determine from CSS URL or default to bootstrap
$currentTheme = 'bootstrap'; // default
if (isset($cssUrl) && str_contains($cssUrl, 'themes/main/assets')) {
    $currentTheme = 'tailwind';
}

$this->layout($currentTheme . '_layouts::app', [
    'title' => $title ?? 'Session',
    'cssUrl' => $cssUrl ?? null,
    'jsUrl' => $jsUrl ?? null,
]);
?>

<div class="container">
    <div class="row">
        <div class="col-md-12">
            <h1><?= $this->escapeHtml($title ?? 'Session') ?></h1>
            <p>Welcome to the <?= $this->escapeHtml($module ?? 'Session') ?> module!</p>
            
            <div class="alert alert-info">
                <strong>Module created successfully!</strong>
                <br>
                This is a sample template for the Session module.
                <br>
                Edit <code>src/Session/templates/index.phtml</code> to customize this page.
            </div>
        </div>
    </div>
</div>
