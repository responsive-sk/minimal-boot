<?php
$layout('bootstrap_layouts::app', [
    'title' => $title,
]);
?>

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-8 text-center">
            <div class="card">
                <div class="card-body">
                    <h1 class="display-1 text-danger">403</h1>
                    <h2 class="mb-4">Access Denied</h2>
                    <p class="lead"><?= $e($message ?? 'You do not have permission to access this resource.') ?></p>
                    
                    <?php if (isset($required_permission)): ?>
                        <p class="text-muted">Required permission: <code><?= $e($required_permission) ?></code></p>
                    <?php endif ?>
                    
                    <div class="mt-4">
                        <a href="/" class="btn btn-primary me-2">Go Home</a>
                        <a href="/dashboard" class="btn btn-outline-secondary">Dashboard</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
