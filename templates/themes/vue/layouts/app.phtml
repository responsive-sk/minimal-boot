<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1"/>
    <meta name="description" content="<?= htmlspecialchars($description ?? 'Cyberpunk Vue Application - Neural Network Interface') ?>"/>
    <meta name="author" content="<?= htmlspecialchars($author ?? 'Cyber Solutions Team') ?>"/>
    <meta name="generator" content="Minimal Boot + Vue.js + Cyberpunk"/>
    <meta name="theme-color" content="#00ffff"/>

    <title><?= htmlspecialchars($title ?? 'CYBER.SOLUTIONS - Neural Network Interface') ?></title>

    <!-- Favicons -->
    <link rel="icon" type="image/png" sizes="32x32" href="/images/app/favicon/favicon-32x32.png"/>
    <link rel="icon" type="image/png" sizes="16x16" href="/images/app/favicon/favicon-16x16.png"/>
    <link rel="manifest" href="/images/app/favicon/site.webmanifest"/>
    <link rel="mask-icon" href="/images/app/favicon/safari-pinned-tab.svg" color="#00ffff"/>
    <meta name="msapplication-TileColor" content="#0a0a0a"/>

    <!-- Vue Cyberpunk Theme CSS -->
    <link rel="stylesheet" href="/themes/vue/assets/style.css"/>

    <!-- Cyberpunk Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com"/>
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>

    <!-- SEO Meta Tags -->
    <meta property="og:title" content="<?= htmlspecialchars($title ?? 'CYBER.SOLUTIONS - Neural Network Interface') ?>"/>
    <meta property="og:description" content="<?= htmlspecialchars($description ?? 'Advanced cyberpunk web solutions with neural network capabilities') ?>"/>
    <meta property="og:type" content="website"/>
    <meta property="og:image" content="/images/app/logo.png"/>

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image"/>
    <meta name="twitter:title" content="<?= htmlspecialchars($title ?? 'CYBER.SOLUTIONS') ?>"/>
    <meta name="twitter:description" content="<?= htmlspecialchars($description ?? 'Cyberpunk Vue interface') ?>"/>
</head>

<body class="h-full antialiased bg-gray-900 text-cyan-400">
    <!-- Page Content -->
    <?= $content ?>
    
    <!-- Fallback content for non-JS users -->
    <noscript>
        <div class="min-h-screen bg-gray-900 flex items-center justify-center">
            <div class="text-center text-cyan-400 p-8 border border-cyan-400 rounded-lg">
                <h1 class="text-4xl font-bold mb-4 text-pink-500">NEURAL LINK REQUIRED</h1>
                <p class="text-xl mb-6">Please enable JavaScript to access the cyberpunk interface.</p>
                <a href="/" class="inline-block px-6 py-3 border-2 border-cyan-400 text-cyan-400 hover:bg-cyan-400 hover:text-gray-900 transition-colors rounded">
                    BASIC MODE
                </a>
            </div>
        </div>
    </noscript>

    <!-- Vue App Script - Load after DOM -->
    <script src="/themes/vue/assets/main.js"></script>
</body>
</html>
